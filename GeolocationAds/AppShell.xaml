<Shell
    x:Class="GeolocationAds.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GeolocationAds"
    xmlns:pages="clr-namespace:GeolocationAds.Pages"
    Shell.BackgroundColor="Red"
    Shell.TabBarIsVisible="False">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:Login}"
        FlyoutItemIsVisible="False"
        Route="login"
        Shell.FlyoutBehavior="Disabled" />

    <!--<TabBar>

    -->
    <!--  Home Tab  -->
    <!--
        <Tab Title="Home">
            <ShellContent Title="Home" ContentTemplate="{DataTemplate local:SearchAd}" />
        </Tab>

    -->
    <!--  Create Ads Tab  -->
    <!--
        <Tab Title="Create">
            <ShellContent Title="Ads" ContentTemplate="{DataTemplate local:CreateAdvertisment}" />
        </Tab>

        <Tab Title="Your Content">

            <ShellContent Title="Ads" ContentTemplate="{DataTemplate local:AdToLocation}" />
        </Tab>

        <Tab Title="Settings">

            <ShellContent Title="Ads" ContentTemplate="{DataTemplate local:AdToLocation}" />
        </Tab>
    </TabBar>-->

    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems" Route="SearchAd">
        <ShellContent Title="Home" ContentTemplate="{DataTemplate pages:SearchAd}" />
        <ShellContent Title="Create" ContentTemplate="{DataTemplate pages:CreateAdvertisment}" />
        <ShellContent Title="Set Location" ContentTemplate="{DataTemplate pages:AdToLocation}" />

    </FlyoutItem>
</Shell>