<?xml version="1.0" encoding="utf-8" ?>
<Grid
    x:Class="GeolocationAds.ViewTemplates.ImageDisplayer"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
    xmlns:headerView="clr-namespace:GeolocationAds.ViewTemplates">

    <ffimageloading:CachedImage Aspect="Fill" Source="{Binding Image.Source}" />

    <Border
        x:Name="myContentStackHeader"
        Grid.Row="0"
        Style="{StaticResource borderOnHeaderControls}">

        <headerView:ContentHeaderView />
    </Border>

    <Border
        x:Name="searchContentStackHeader"
        Grid.Row="0"
        Style="{StaticResource borderOnHeaderControls}">

        <headerView:HomeContentItemHeaderView />
    </Border>

    <ActivityIndicator
        Grid.Row="0"
        IsRunning="{Binding IsLoading}"
        IsVisible="{Binding IsLoading}"
        Style="{StaticResource globalLoading}" />
</Grid>