<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GeolocationAds.Pages.CreateAdvertisment"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GeolocationAds.CustomContentView"
    xmlns:templateSelector="clr-namespace:GeolocationAds.DataTemplates"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="Create Your Content"
    Routing.Route="CreateAdvertisement">

    <ContentPage.Resources>


        <DataTemplate x:Key="ImageDataTemplate2">
            <Frame Style="{StaticResource cardFrame}">
                <VerticalStackLayout>

                    <Button
                        Command="{Binding RemoveCommand}"
                        CommandParameter="{Binding CurrentAdvertisement}"
                        IsVisible="{Binding IsLoading, Converter={StaticResource IsEnableConverter}}"
                        Style="{StaticResource XButton}"
                        Text="X" />

                    <Frame Style="{StaticResource imageFrame}">

                        <Image
                            Aspect="AspectFill"
                            HeightRequest="300"
                            IsAnimationPlaying="True"
                            Source="{Binding ContentType.Content, Converter={StaticResource ByteArrayToImageSourceConverter}, Mode=TwoWay}"
                            WidthRequest="400" />
                    </Frame>

                    <ActivityIndicator
                        IsRunning="{Binding IsLoading}"
                        IsVisible="{Binding IsLoading}"
                        Style="{StaticResource globalLoading}" />
                </VerticalStackLayout>
            </Frame>
        </DataTemplate>

        <DataTemplate x:Key="VideoDataTemplate2">

            <VerticalStackLayout>

                <Button
                    Command="{Binding RemoveCommand}"
                    CommandParameter="{Binding CurrentAdvertisement}"
                    IsVisible="{Binding IsLoading, Converter={StaticResource IsEnableConverter}}"
                    Style="{StaticResource XButton}"
                    Text="X" />

                <!--<WebView
                        x:Name="SelectedVideo"
                        HeightRequest="300"
                        Source="{Binding ContentType.Content, Converter={StaticResource ByteArrayToVideSourceConverter}}"
                        WidthRequest="400" />-->

                <toolkit:MediaElement
                    
                    Aspect="AspectFill"
                    StateChanged="MediaElement_StateChanged"
                    ShouldKeepScreenOn="True"
                    Source="{Binding MediaSource}" />

                <ActivityIndicator
                    IsRunning="{Binding IsLoading}"
                    IsVisible="{Binding IsLoading}"
                    Style="{StaticResource globalLoading}" />
            </VerticalStackLayout>
        </DataTemplate>

        <templateSelector:ContentTypeTemplateSelector
            x:Key="typeSelector"
            ImageDataTemplate="{StaticResource ImageDataTemplate2}"
            VideoDataTemplate="{StaticResource VideoDataTemplate2}" />
    </ContentPage.Resources>

    <ScrollView x:Name="ScrollView01">


        <Frame Style="{StaticResource cardFrame}">

            <VerticalStackLayout>

                <CollectionView ItemsSource="{Binding ValidationResults}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout>
                                <Label Style="{StaticResource ErrorLabelStyle}" Text="{Binding ErrorMessage}" />
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Label
                    Margin="5"
                    FontSize="30"
                    Style="{StaticResource globalLabel}"
                    Text="Register Content" />

                <Label Style="{StaticResource globalLabel}" Text="Title:" />
                <local:CustomEntryContentView Placeholder="Title" Text="{Binding Model.Title}" />

                <Label Style="{StaticResource globalLabel}" Text="Description:" />
                <local:CustomEntryContentView Placeholder="Description" Text="{Binding Model.Description}" />

                <Label Style="{StaticResource globalLabel}" Text="Type:" />
                <local:CustomPickerContentView
                    HorizontalOptions="FillAndExpand"
                    ItemDisplayBindingPath="Value"
                    ItemsSource="{Binding AdTypesSettings}"
                    SelectedItem="{Binding SelectedAdType}" />

                <!--<Label Style="{StaticResource globalLabel}" Text="Expiration Date:" />
                <local:CustomDatePickerContentView SelectedDate="{Binding Model.ExpirationDate}" />-->

                <!--  ... Choose File ...  -->
                <Button
                    x:Name="UploadButton"
                    Command="{Binding UploadContentCommand}"
                    Style="{StaticResource globalButton}"
                    Text="Choose Image">

                    <Button.Shadow>
                        <Shadow
                            Brush="Black"
                            Opacity="0.5"
                            Radius="40"
                            Offset="5,5" />
                    </Button.Shadow>
                </Button>

                <CarouselView ItemTemplate="{StaticResource typeSelector}" ItemsSource="{Binding ContentTypesTemplate}" />

                <!--  Selected Image or Video  -->
                <!--
                <Frame IsVisible="{Binding VisualType, Converter={StaticResource ImageOrVideoCheckConverter}, ConverterParameter=Video}" Style="{StaticResource imageFrame}">
                    <WebView
                        x:Name="SelectedVideo"
                        HeightRequest="300"
                        Source="{Binding Video}"
                        WidthRequest="400" />
                </Frame>

                -->
                <!--  Selected Image or Video  -->
                <!--
                <Frame IsVisible="{Binding VisualType, Converter={StaticResource ImageOrVideoCheckConverter}, ConverterParameter=Image}" Style="{StaticResource imageFrame}">
                    <Image
                        Aspect="AspectFill"
                        HeightRequest="300"
                        IsAnimationPlaying="{Binding IsAnimation}"
                        Source="{Binding Image.Source, Mode=TwoWay}"
                        WidthRequest="400" />
                </Frame>-->

                <!--  Submit Button  -->
                <Button
                    x:Name="SubmitButton"
                    Command="{Binding SubmitCommand}"
                    CommandParameter="{Binding Model}"
                    IsVisible="{Binding IsLoading, Converter={StaticResource IsEnableConverter}}"
                    Style="{StaticResource globalButton}"
                    Text="Save">

                    <Button.Shadow>
                        <Shadow
                            Brush="Black"
                            Opacity="0.5"
                            Radius="40"
                            Offset="5,5" />
                    </Button.Shadow>
                </Button>

                <ContentView
                    x:Name="LoadingIndicatorContainer"
                    IsVisible="{Binding IsLoading}"
                    VerticalOptions="FillAndExpand">
                    <ContentView.Content>
                        <ActivityIndicator IsRunning="{Binding IsLoading}" Style="{StaticResource globalLoading}" />
                    </ContentView.Content>
                </ContentView>
            </VerticalStackLayout>
        </Frame>

    </ScrollView>
</ContentPage>